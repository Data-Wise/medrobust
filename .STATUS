status: released
priority: P0
progress: 100
next: CRAN-PREP COMPLETE on feature/cran-prep (v0.2.1, pushed). Strict R CMD check --as-cran --run-donttest = 0E/0W/1 NOTE (new submission only; >5s example NOTE cleared by trimming bound_ne to a single ci_method="analytic" solve @ n=8000 + simulate_dm_data n_grid=10). 4 medfit-derived manual-review fixes (46b501f), n=8000/bound_ci examples (63305ca), _freeze cache committed + detritus gitignored (3fe1b43). win-builder 0.2.1 clean. >>> ONLY STEP LEFT: run devtools::submit_cran() (needs TTY) <<<. Prior: RELEASED v0.2.0 (3 bugs fixed + bound_ci IM CIs; PR #7, tag v0.2.0). Sibling: pmed Hopper confirmatory-run SPEC + RUN.md ready (feature/bootstrap-mbco) — author runs on Hopper via ssh.
blocked: none.
worktrees: (2026-06-14) ~/.git-worktrees/medrobust/bound-ne-robust [feature/bound-ne-robust off cran-prep] — M2a pilot bug fixes: (a) graceful infeasible NA-result + medrobust_infeasible condition, (b) analytic-CI is.finite guard; +2 regression tests; test 224/0/1, --as-cran 0E/0W (commit 3a9ee46), ready (not merged). ~/.git-worktrees/medrobust/gesthtn [feature/gesthtn-example] — generated man/gesthtn.Rd; --as-cran 0E/0W (commit 163e300), ready. NOTE gesthtn vignette bound_ne chunk ~40s (cost is the grid g-computation on 5000 rows, NOT the analytic CI — confirmed by profiling); this is a VIGNETTE not an Rd example, so it does NOT trigger the >5s-example NOTE and --as-cran is clean. Optional polish: commit a quarto _freeze entry for gesthtn-bounds (the repo's pattern for its other expensive vignettes) — must be generated via a full pkgdown/project render, not a single-file `quarto render` (which won't write _freeze here; no `project:` type in vignettes/_quarto.yml). Site-library install side-effect RESOLVED: reinstalled clean cran-prep v0.2.1 build (heals_data only).
type: r
last_updated: 2026-06-14

# ═══════════════════════════════════════════════════════════════════
# Below is the original .STATUS content (preserved for reference)
# ═══════════════════════════════════════════════════════════════════

📦 medrobust - Robust Causal Mediation Analysis
────────────────────────────────────────────────

📍 LOCATION
~/projects/r-packages/active/medrobust/

⏰ LAST UPDATED
2026-05-09

────────────────────────────────────────────────

🎯 CURRENT STATUS
Priority: P0
Status: active
Progress: 65

────────────────────────────────────────────────

📋 DESCRIPTION
Sensitivity analysis for causal mediation effects
under differential misclassification (recall bias,
outcome-dependent measurement error).

Implements partial identification bounds without
requiring gold-standard measurements.

Version: 0.1.0.9000

────────────────────────────────────────────────

Next: Complete vignettes, prepare for CRAN submission
Blocked: None

📋 Recent Housekeeping (2026-05-09):
- ✅ Renamed default branch from claude/check-measurement-error-project-...
  to `main` (resolved branch hygiene tech debt)
- ✅ Switched origin URL from SSH to HTTPS (gh-token auth)
- ✅ R CMD build verified clean (medrobust_0.1.0.9000.tar.gz)
- ✅ All 22 R/ files parse cleanly

────────────────────────────────────────────────

✅ COMPLETED
- S7 class system
- NDE/NIE partial identification bounds
- Data-driven falsification tests
- Sensitivity analysis grid
- Bootstrap inference (percentile + BCa)
- Publication-ready visualizations
- Mediator misclassification support
- Exposure misclassification support

────────────────────────────────────────────────

📁 KEY FILES
R/              # 50+ source files
tests/testthat/ # Test suite
man/            # Documentation
vignettes/      # Method tutorials

────────────────────────────────────────────────

🔗 DEPENDENCIES
Imports: S7, dplyr, ggplot2, stats, rlang, parallel
Part of: mediationverse ecosystem
