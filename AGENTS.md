# AGENTS.md for medrobust Package

All agent guidance for this repository lives in **[`CLAUDE.md`](CLAUDE.md)**.
Read it in full before working here — it is the single source of truth for
package status, coding standards, architecture, testing, and repository
conventions. Authoritative project state is in `.STATUS`.

This file is intentionally a pointer only, so the two files cannot drift.
Do not copy content here; edit `CLAUDE.md` instead.
